import { createSlice } from "@reduxjs/toolkit";


export const typingSlice = createSlice({
    name : 'typing',
    initialState : {
        value : 0
    },
    reducers:{

    }
})
//export const {reducerfonk} = typingSlice.actions
export default typingSlice.reducer